#include <stdio.h>

/**
  * main - Outputs numbers 0 to 9 without using char type
  *
  * Return: Always (Success)
  */
int main(void)
{
	int num;

	for (num = '0'; n <= '9'; num++)
	{
		putchar(num);
	}

	putchar('\n');

	return (0);
}
